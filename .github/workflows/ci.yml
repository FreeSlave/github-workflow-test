name: build

on: [push]

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [macos-latest, macos-10.15]
    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Build
      run: |
        clang++ main.cpp && ./a.out

